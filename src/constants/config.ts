// Timeline and zoom configuration
export const MIN_TIMELINE_MS = 15000; // 15 seconds default timeline length
export const ZOOM_MIN = 0.25;
export const ZOOM_MAX = 8;
export const ZOOM_FACTOR = 1.5; // Keyboard/button zoom multiplier
export const PINCH_ZOOM_FACTOR = 1.05; // Touch pinch zoom multiplier

// Polling intervals
export const POSITION_POLL_MS = 30;
export const LEVEL_POLL_MS = 50;

// Track limits
export const TRACK_LIMIT = 8;

// Transport
export const SKIP_DELTA_MS = 15000; // 15 second skip

// Trim mode
export const TRIM_MIN_SELECTION_MS = 100;

// Auto-save
export const AUTO_SAVE_DEBOUNCE_MS = 300;
